const nextConfig = {
  output: 'standalone',
  images: {
    domains: [
      'connection-with-nature.s3.us-east-1.amazonaws.com',
      'tu-dominio.vercel.app'
    ],
  },
}

export default nextConfig;
